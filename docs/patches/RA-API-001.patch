--- a/backend/app/api/routes.py
+++ b/backend/app/api/routes.py
@@
 @router.post("/documents/{file_id}/reindex", response_model=UploadResponse)
-async def reindex_document(file_id: str, background_tasks: BackgroundTasks):
+async def reindex_document(file_id: uuid.UUID, background_tasks: BackgroundTasks):
     """Rebuild vector index for an uploaded PDF document."""
+    document_id = str(file_id)
     settings = get_settings()
-    reports = list(settings.reports_path.glob(f"{file_id}_*.pdf"))
+    reports = list(settings.reports_path.glob(f"{document_id}_*.pdf"))
@@
-    file_name = file_path.name.replace(f"{file_id}_", "", 1)
+    file_name = file_path.name.replace(f"{document_id}_", "", 1)
 
-    document_status[file_id] = {
+    document_status[document_id] = {
@@
-        file_id,
+        document_id,
@@
-        file_id=file_id,
+        file_id=document_id,
@@
 @router.get("/documents/{file_id}")
-async def get_document(file_id: str):
+async def get_document(file_id: uuid.UUID):
     """Get information about a specific document."""
+    document_id = str(file_id)
     settings = get_settings()
@@
-    reports = list(settings.reports_path.glob(f"{file_id}_*"))
+    reports = list(settings.reports_path.glob(f"{document_id}_*"))
@@
-        status_info = document_status.get(file_id, {"status": DocumentStatus.PENDING})
+        status_info = document_status.get(
+            document_id,
+            {"status": DocumentStatus.PENDING},
+        )
@@
-            "file_id": file_id,
-            "file_name": file_path.name.replace(f"{file_id}_", ""),
+            "file_id": document_id,
+            "file_name": file_path.name.replace(f"{document_id}_", ""),
@@
-    uploads = list(settings.uploads_path.glob(f"{file_id}_*"))
+    uploads = list(settings.uploads_path.glob(f"{document_id}_*"))
@@
-            "file_id": file_id,
-            "file_name": file_path.name.replace(f"{file_id}_", ""),
+            "file_id": document_id,
+            "file_name": file_path.name.replace(f"{document_id}_", ""),
@@
 @router.delete("/documents/{file_id}")
-async def delete_document(file_id: str):
+async def delete_document(file_id: uuid.UUID):
     """Delete a document and its associated data."""
+    document_id = str(file_id)
     settings = get_settings()
@@
-    reports = list(settings.reports_path.glob(f"{file_id}_*"))
+    reports = list(settings.reports_path.glob(f"{document_id}_*"))
@@
-        vector_store.delete_by_file_id(file_id)
+        vector_store.delete_by_file_id(document_id)
@@
-    if file_id in document_status:
-        del document_status[file_id]
+    if document_id in document_status:
+        del document_status[document_id]
@@
-    uploads = list(settings.uploads_path.glob(f"{file_id}_*"))
+    uploads = list(settings.uploads_path.glob(f"{document_id}_*"))
@@
-        return {"status": "deleted", "file_id": file_id}
+        return {"status": "deleted", "file_id": document_id}

--- a/backend/tests/test_api/test_routes.py
+++ b/backend/tests/test_api/test_routes.py
@@
 from app.main import app
 
+EXISTING_FILE_ID = "11111111-1111-4111-8111-111111111111"
+MISSING_FILE_ID = "22222222-2222-4222-8222-222222222222"
+
@@
-        response = client.get("/api/documents/nonexistent-id")
+        response = client.get(f"/api/documents/{MISSING_FILE_ID}")
@@
-        response = client.delete("/api/documents/nonexistent-id")
+        response = client.delete(f"/api/documents/{MISSING_FILE_ID}")
@@
-        pdf_path = reports_path / "file123_test.pdf"
+        pdf_path = reports_path / f"{EXISTING_FILE_ID}_test.pdf"
@@
-        response = client.post("/api/documents/file123/reindex")
+        response = client.post(f"/api/documents/{EXISTING_FILE_ID}/reindex")
@@
-        assert data["file_id"] == "file123"
+        assert data["file_id"] == EXISTING_FILE_ID
@@
-            "file123",
+            EXISTING_FILE_ID,
@@
-        response = client.post("/api/documents/missing/reindex")
+        response = client.post(f"/api/documents/{MISSING_FILE_ID}/reindex")

--- a/backend/tests/test_integration/test_api_e2e.py
+++ b/backend/tests/test_integration/test_api_e2e.py
@@
 os.environ.setdefault("APP_ENV", "testing")
 
+MISSING_FILE_ID = "22222222-2222-4222-8222-222222222222"
+
@@
-        response = client.get("/api/documents/nonexistent-id-12345")
+        response = client.get(f"/api/documents/{MISSING_FILE_ID}")
@@
-        response = client.delete("/api/documents/nonexistent-id-12345")
+        response = client.delete(f"/api/documents/{MISSING_FILE_ID}")
